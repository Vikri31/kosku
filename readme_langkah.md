setiap mau ngoding, ketikkan hal berikut agar bisa mengambil versi terbaru yang sudah di push:
git pull origin main

1. setelah melakukan git clone, lalu buat file .env yang isinya sudah saya share di whatsapp

2. ambil dependency flutter dengan mengetikkan. jalankan di terminal
flutter pub get

3. hubungkan supabase cli. jalankan di terminal
supabase login
supabase link --project-ref lscr... (copas / cari di whatsapp)

4. wajib, sebelum mulai codingan. ambil databasenya dulu dengan mengetikkan
git pull origin main

5. setelah selesai, push ke github.
git add .
git commit -m "feat: isi deskripsi update / perubahan"
git push origin main